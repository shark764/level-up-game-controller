import * as GAME_CONTROLLER_EVENTS from './game-controller';
import * as DEVICE_EVENTS from './device';

export const EVENTS = {
  GAME_CONTROLLER: GAME_CONTROLLER_EVENTS,
  DEVICE: DEVICE_EVENTS,
};
